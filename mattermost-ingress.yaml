apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: mattermost
spec:
  rules:
  - host: chat.$(minikube ip).nip.io
    http:
      paths:
      - backend:
          serviceName: mattermost
          servicePort: 8065
